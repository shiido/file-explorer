{
   "dependencies": {
      "chalk": "^4.1.2",
      "node-cmd": "^5.0.0"
   },
   "scripts": {
      "postinstall": "(cd file-explorer-backend && npm install); (cd file-explorer-frontend && npm install); "
   }
}
